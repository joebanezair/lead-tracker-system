import{FiDatabase,FiUsers,FiCopy,FiAlertTriangle,FiActivity,FiStar,FiMessageSquare}from'react-icons/fi';import PageHeader from'../components/layout/PageHeader.jsx';import StatCard from'../components/dashboard/StatCard.jsx';export default function DashboardPage({stats,events}){const cards=[[FiDatabase,'Total Leads',stats.total],[FiUsers,'Unique Leads',stats.unique],[FiCopy,'Duplicates',stats.duplicates],[FiAlertTriangle,'Invalid',stats.invalid]];return <><PageHeader title="Lead Dashboard" description="Import, deduplicate and manage lead datasets."/><section className="cards">{cards.map(([I,l,v])=><StatCard key={l} icon={I} label={l} value={v}/>)}</section><section className="grid"><div className="panel"><h3>Lead workflow</h3><div className="flow">Upload → Map Columns → Preview → Background Import → Normalize → Validate → Dedupe → Metadata → Master Leads → Export</div><div className="features"><span><FiStar/> Ratings</span><span><FiMessageSquare/> Comments</span><span><FiActivity/> Realtime</span></div></div><div className="panel"><h3><FiActivity/> Realtime Activity</h3>{events.length?events.map((e,i)=><p className="event" key={i}>{e}</p>):<p>Waiting for WebSocket events…</p>}</div></section></>}