import{useEffect,useRef}from'react';import{FiMail,FiLock}from'react-icons/fi';const GOOGLE_CLIENT_ID=import.meta.env.VITE_GOOGLE_CLIENT_ID;export default function LoginPage({onGoogleLogin}){const googleButton=useRef(null);useEffect(()=>{if(!GOOGLE_CLIENT_ID)return;const init=()=>{if(!window.google||!googleButton.current)return;window.google.accounts.id.initialize({client_id:GOOGLE_CLIENT_ID,callback:r=>onGoogleLogin(r.credential)});window.google.accounts.id.renderButton(googleButton.current,{theme:'outline',size:'large',width:360,text:'continue_with'})};if(window.google)init();else{const s=document.createElement('script');s.src='https://accounts.google.com/gsi/client';s.async=true;s.onload=init;document.head.appendChild(s)}},[onGoogleLogin]);return <div className="auth-shell"><section className="auth-card"><h1>Welcome to LeadTracker</h1><p>Sign in to manage, deduplicate and track your leads.</p><label><FiMail/> Email</label><input type="email" placeholder="you@example.com"/><label><FiLock/> Password</label><input type="password" placeholder="••••••••"/><button className="auth-primary">Sign In</button><div className="divider"><span>OR</span></div>{GOOGLE_CLIENT_ID?<div ref={googleButton} className="google-button"/>:<button className="google-placeholder" disabled>G&nbsp;&nbsp; Continue with Google</button>}<small>{!GOOGLE_CLIENT_ID&&'Google sign-in will activate after VITE_GOOGLE_CLIENT_ID is configured.'}</small></section></div>