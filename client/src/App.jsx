import React,{useEffect,useState}from'react';import{io}from'socket.io-client';import{FiUploadCloud,FiUsers,FiDatabase,FiCopy,FiAlertTriangle,FiDownload,FiActivity,FiStar,FiMessageSquare}from'react-icons/fi';
const API=import.meta.env.VITE_API_URL||'http://localhost:5000';
export default function App(){const[events,setEvents]=useState([]);const[stats,setStats]=useState({total:0,unique:0,duplicates:0,invalid:0});
useEffect(()=>{const s=io(API);s.on('connect',()=>setEvents(x=>['Realtime connection established',...x].slice(0,8)));s.on('import:progress',d=>setEvents(x=>[`Import ${d.batchId}: ${d.progress}%`,...x].slice(0,8)));s.on('lead:created',()=>setStats(x=>({...x,total:x.total+1,unique:x.unique+1})));return()=>s.disconnect()},[]);
const cards=[[FiDatabase,'Total Leads',stats.total],[FiUsers,'Unique Leads',stats.unique],[FiCopy,'Duplicates',stats.duplicates],[FiAlertTriangle,'Invalid',stats.invalid]];
return <div className="app"><aside><h2>LeadTracker</h2><nav>Dashboard<br/>Leads<br/>Import Leads<br/>Import History<br/>Duplicates<br/>Invalid Leads<br/>Export Center</nav></aside><main><header><div><h1>Lead Dashboard</h1><p>Import, deduplicate and manage lead datasets.</p></div><button><FiUploadCloud/> Import Leads</button></header><section className="cards">{cards.map(([I,l,v])=><article key={l}><I/><span>{l}</span><strong>{v.toLocaleString()}</strong></article>)}</section><section className="grid"><div className="panel"><h3><FiUploadCloud/> Import up to 100,000 leads</h3><div className="drop"><FiUploadCloud size={38}/><b>Drag XLSX, XLS or CSV here</b><span>Flexible column mapping supported</span><button>Choose file</button></div><div className="actions"><button><FiDownload/> XLSX Template</button><button><FiDownload/> CSV Template</button></div></div><div className="panel"><h3><FiActivity/> Realtime Activity</h3>{events.length?events.map((e,i)=><p className="event" key={i}>{e}</p>):<p>Waiting for WebSocket events…</p>}</div></section><section className="panel"><h3>Lead workflow</h3><div className="flow">Upload → Map Columns → Preview → Background Import → Normalize → Validate → Dedupe → Metadata → Master Leads → Export</div><div className="features"><span><FiStar/> Ratings</span><span><FiMessageSquare/> Comments</span><span><FiActivity/> Realtime updates</span></div></section></main></div>}